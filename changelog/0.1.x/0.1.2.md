---
summary: "Scope npm package to @cyanheads/census-mcp-server"
breaking: false
security: false
---

# 0.1.2 — 2026-05-24

## Changed

- **package name**: `census-mcp-server` → `@cyanheads/census-mcp-server` — scoped to the `@cyanheads` org for consistent npm publishing
- **`bundle` script**: switched from `npm run build` to `bun run build` (matches framework convention)
- **`author` field**: normalized to `cyanheads <casey@caseyjhand.com>` format (matches other servers in the ecosystem)
- **`server.json` `packages[].identifier`**: updated to `@cyanheads/census-mcp-server` to match the scoped package name
- **README install badges**: Cursor config updated to `@cyanheads/census-mcp-server` via `npx`; VS Code config updated to scoped package; npm badge now points to scoped package URL
